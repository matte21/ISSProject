A Node.js script started manually on a terminal (by whoever runs this demo) 
sends a message to a QActor. The format of the message is hardcoded in the
script.

To run the demo, first run the jar, then open a terminal
in the root directory of the project and launch sendMsg.sh